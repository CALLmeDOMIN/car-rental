import {
    IconMapPins,
    IconPlaneDeparture,
    IconBuildingSkyscraper,
    IconHotelService,
} from "@tabler/icons-react";

export default function IconRender(params: { params: { icon: string } }) {
    const icons = {};

    return <div />;
}
